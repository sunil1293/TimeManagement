<% if @description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@description.errors.count, "error") %> prohibited this description from being saved:</h2>

      <ul>
      <% @description.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
      <%= f.label :select_project %><br/>
      <%= f.select :select_project, Project.all.map {|p| [p.project_name, p.id]}, :autofocus => true %>
  </div>

  <div>
      <%= f.label :select_task %> <br/>
      <%= f.select :select_task, Task.all.map {|p| [p.task, p.id]} %>
  </div>
  <div>
    <%= f.label :status_on %><br>
    <%= f.date_field :status_on %>
  </div>
  <div>
    <%= f.label :number_of_hours %><br>
    <%= f.text_field :number_of_hours %>
  </div>
  <div>
    <%= f.label :today_status %><br>
    <%= f.text_area :today_status %>
  </div>


